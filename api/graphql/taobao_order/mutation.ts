import { extendType } from "nexus";
import { errors, throwError } from "../../utils/error";

export const mutation_taobao_order = extendType({
    type: "Mutation",
    definition(t) {
        // t.field("process", {
        //     type: "Int"
        // })
    }
});